{% extends 'base.html.twig' %}

{% block title %}Hello CompaniesController!{% endblock %}

{% block body %}


<div class="container">
    <div class="row justify-content-center">
        <div class="col-8">
            <h1>Détails</h1  1>
            <p>Nom : {{company.nom}}</p>
            <p>Sigle : {{company.sigle}}</p>
            <p>Employés : {{company.employes}}</p>
            <a href="{{ path('app_companies')}}" class="btn btn-danger">Retour à la liste</a>
        </div>
    </div>
</div>


{% endblock %}